<h1>Tweets</h1>
<ul>
  <% @tweets.each do |t| %>
    <li>
      <div>
        <h4><%= t.user.name %></h4>
        <p><%= t.content %></p>
      </div>
      <a href="/tweets/<%= t.id %>"><%= t.user.name %></a>
    </li>
  <% end %>

</ul>