<h2>Welcome, <%= current_user.username %>!</h2>

<h2>All Tweets</h2>
<ul>
  <% @tweets.each do |tweet| %>
    <li>
      <a href="/tweets/<%= tweet.id %>"><%= tweet.content %> - <%= tweet.user.username %></a>
    </li>
  <% end %>
</ul>