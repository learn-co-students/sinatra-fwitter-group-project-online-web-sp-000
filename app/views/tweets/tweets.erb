<h1>Welcome, <%= Helpers.current_user(session).username %></h1>
	<h2>All tweets</h2>

	<hr>
	<% @tweets.each do |tweet| %>
	  <h4> <%= tweet.user.username %> : <%= tweet.content %> <h4>
	  <hr>
	<% end %>
