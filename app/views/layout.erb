<!doctype html>
<html>
    <head>
        <title>Fwitter</title>
    </head>
    <body>
        <nav>
            <% if logged_in? %>
                <h2>Welcome, <a href="/users/<%= current_user.id %>"><%= current_user.username %></a></h2>
                <a href="/tweets/new">Post</a>
                <a href="/tweets">Tweets</a>
                <a href="/logout">Log Out</a>

            <% else %>
                <a href="/signup">Sign Up</a>
                <a href="/login">Sign In</a>
            <%end %>
        </nav>

        <%= yield %>

    </body>
</html>
