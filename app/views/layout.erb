<!doctype html>
<html>
  <head>
    <title>Fwitter</title>
  </head>
  <body>
    <nav>
      <% if logged_in? %>
        <a href="/tweets/new"> Compose New Tweet </a>
        <a href="/logout">Log Out</a>
        <p>Welcome, <%=current_user.username%></p>
        <% else %>
        <a href="/login">Login</a>
        <a href="/signup">Sign Up</a>
      <%end%>
    <div class="container">
      <h1>Welcome to Fwitter!</h1>
<%=yield%>
    </div>

  </body>
</html>
