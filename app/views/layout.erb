<!DOCTYPE html>
<html>
  <head>
    <title>Fwitter</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.7.2/animate.min.css"
    />
  </head>
  <style>
    html {
      font-family: Arial, Helvetica, sans-serif;
    }
    body {
      margin: 0;
    }
    .button {
      display: block;
    }
    h3 {
      text-align: center;
      color: rgba(29, 161, 242, 1);
    }
    .user-form {
      max-width: 80%;
      margin: 0 auto 50px auto;
    }
    .user-form label {
      display: block;
      color: rgb(100, 100, 100);
      margin-top: 10px;
    }
    .user-form input {
      border: 1px solid lightgrey;
      display: inline-block;
      width: 100%;
      height: 30px;
      margin: 10px 0;
      font-size: 1em;
      padding-left: 10px;
    }
    .form-btn {
      margin: 30px auto;
      padding: 15px 20px;
      width: 75%;
      border-radius: 10px;
      border: none;
      background-color: rgba(29, 161, 242, 0.75);
      color: white;
      font-size: 1.1em;
      font-weight: 700;
      text-align: center;
    }
    .nav-links {
      position: absolute;
      top: 30px;
      right: 50px;
    }
    .nav-links a {
      text-decoration: none;
      color: white;
      margin-left: 25px;
    }
    a.new-tweet-btn {
      color: rgba(29, 161, 242, 1);
      background-color: white;
      padding: 6px 10px;
      border-radius: 5px;
      box-shadow: 6px 6px 6px rgba(0, 0, 0, 0.2);
    }
  </style>
  <body>
    <div class="container">
      <nav
        style="background-color: rgba(29,161,242,1.00); color: white; padding: 5px 0;"
      >
        <a href="/tweets" style="text-decoration: none; color: white"
          ><h1 style="text-align: center">Welcome to Fwitter!</h1></a
        >

        <% if logged_in? %>
        <div class="nav-links">
          <a href="/tweets/new" class="new-tweet-btn">new tweet</a>
          <a href="/logout">log out</a>
        </div>
        <% end %>
      </nav>
      <main>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
